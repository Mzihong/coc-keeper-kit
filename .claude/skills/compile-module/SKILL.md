---
name: compile-module
description: Compile everything generated for one closed arc — world, cast, scenes, clues — into a single linear, readable module text the Keeper can flip through at the table like a published adventure, following the shape of the rulebook's own Chapter 15. Runs a gap report first (which NPCs are still stubs, which scenes have no read-aloud prose, which clues have no landing scene) so the Keeper can fill real holes before compiling over them. Use once an arc has actually been played and its events are in canon-log.md — "compile this arc into something readable", "turn this into a module", "整理成一份能读的模组", "编译模组". Not for pre-session prep (use design-scenario / templates/session-prep.md instead) and not a rebuild of the scene web (templates/scenario.md stays the design document). Writes campaigns/<slug>/module/.
version: "1.0.0"
---

# Compile Module

Read **`core/16-compile-module.md`** and follow it, including its Quality bar.

Read `core/00-how-to-run.md` first if you haven't this session.

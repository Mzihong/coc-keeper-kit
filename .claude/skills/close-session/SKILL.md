---
name: close-session
description: Close out a working session that changed the kit itself — a spec, a convention, reference/ structure, a script — so WORKLOG.md, CHANGELOG.md, affected README.md files, and the reverse index all stay in sync, and any counts/paths/enumerations written into the session log get grep-verified against actual repo state rather than trusted from memory. Use whenever a maintenance session (not tied to a formal update_plan/ plan, not filing third-party material) is wrapping up — "close out this session", "write a work log", "收尾", "生成工作日志". Not for finishing a plan file (use update_plan/README.md's 完结清单) or filing archived material (use core/14-archive-reference.md step 7) — both are more specific supersets of this checklist.
version: "1.0.0"
---

# Close Out a Working Session

Read **`core/15-close-session.md`** and follow it, including its Quality bar.

If this session is finishing an `update_plan/PNN-*.md` plan, or filing third-party material into
`reference/decks/` or `reference/sourcebooks/`, stop and use that flow's own closing checklist
instead — `core/15` points to both and is a superset for everything else.

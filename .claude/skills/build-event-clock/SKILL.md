---
name: build-event-clock
description: Build or update a Call of Cthulhu campaign's EVENT CLOCK — the doom track of what the threat achieves if the investigators do nothing, plus the trigger table of what fires in response to what the players do and how the world state changes. Use whenever the user asks about timing, pressure, escalation, consequences, or triggers ("what happens if they ignore it", "how does this escalate", "build the timeline", "what if they kill the cult leader", "add a trigger for when they find the ledger"), or when a campaign needs standing world state rather than a single scenario's plot. Writes campaigns/<slug>/world/event-clock.md. For a single mystery's structure use design-scenario.
version: "1.0.0"
---

# Build Event Clock

Read **`core/05-event-clock.md`** and follow it.

Read `core/00-how-to-run.md` first if you haven't this session.

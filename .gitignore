# Local Claude Code state (skills here are shared on disk; commit intentionally if wanted)
.claude/settings.local.json

# Scratch / exports
/exports/
*.pdf
*.docx
*.tmp

# reference/_source/ —— 第三方原件与从中抽出的素材。
# 2026-08-04 改:**原件不入库,抽出的文本与图入库**(此前是整目录忽略)。
# 原件(.pdf / .docx)已被上面两条挡住,这里不再需要整目录规则;
# 边界与理由见 reference/_source/README.md。

# OS noise
Thumbs.db
.DS_Store
desktop.ini

# Python
__pycache__/

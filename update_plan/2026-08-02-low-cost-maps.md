# Update Plan — 低成本地图方案

> 日期:2026-08-02
> 状态:进行中(阶段 0-1 已完成,阶段 1 于 2026-08-07 提交 `090cd3c`;阶段 3 已拍板
> 砍掉,不做;阶段 2/2b 于 2026-08-08 完成并提交 `d56c0dc`,唯一未勾的是"实测 token 差
> 回填 3–5× 估算"
> ——需要一次真实的功能二生成才能测,仓库里目前没有在跑的战役,**非阻塞**,留到下次
> Keeper 真正生成互动场景图时回填。计划本体因此暂不整份归档,收尾清单其余项已走完)
> 来源:从 `Archived/2026-08-02-conventions-gaps.md` §4 拆出独立跟踪(2026-08-02)
> 关联:P1 第三章邪教关系图(阶段 0 要还的债);
> `2026-08-04-town-anatomy-from-arkham.md`(P10)阶段 2 的文字地图卡是本计划的输入格式

## 问题

模组需要图,但纯 token 生成位图或精细 SVG **成本高且不可维护**——图一改就得整张重生,
diff 不可读,三模型(Claude / Gemini / ChatGPT)之间也无法复用。

## 已定案(2026-08-04)

原来的三个待拍板问题,Keeper 已答:

**1. 视觉风格 → 纯几何线框,不做仿手绘。**
参考样本是阿卡姆资料里的地图,现存 `reference/_source/arkham-maps/`(20 张)。
**2026-08-04 晚起这 20 张已入库**(此前是 gitignore 本地件),文件名同时按 ASCII 硬约定
从中文改英文——**下表已是新名**,新旧对照见 `reference/_source/README.md`。
**书里有三种图,体例各不相同,别混:**

| 图种 | 样本 | 特征 |
|---|---|---|
| **城市/区域图** | `district-downtown` `district-campus-east` 等 12 张(`district-*` / `region-*` / `city-*`) | 斜视鸟瞰、手绘建筑、街名斜排、**编号圆圈**(几十个地点写不下名字) |
| **室内平面图** | `interior-1/2/4`(**4 = 密大图书馆**) | **正投影**、灰底白房间、**粗实心黑墙**、**房间名直接写在房间里** |
| **站点/庄园图** | `site-crowninshield-manor` `exterior-1/2` | 自由轮廓 + 纹理填充 + 手写标签引线、**大片留白**(农场那张只有 4 个元素) |

> **2026-08-04 更正:** 本节初稿写"书里根本没有室内平面图",那是只看了体积最大的几张
> (全是城区图)得出的错误结论。实际有三栋建筑的完整多层平面图,**下面阶段 1 的渲染
> 规格就是从它们读出来的**,不是凭空定的。

移植取舍:

- ✅ **灰阶、留白、粗实心黑墙(外墙粗内墙细)**——照抄
- ✅ **编号圆圈**——只用在**城市/区域尺度**;室内图按书里的做法**把房间名写在房间里**
- ❌ **斜视投影**——不做,正投影更好画也更好读(室内图本来就是正投影)
- ❌ **纹理填充**(森林点簇、果园网格、葡萄藤横纹)——不做,改用浅灰形状 + 标签文字。
  信息一点没少,那些点是纯美术
- ❌ **手绘抖动线条**——见下,已否决

**2. DSL 范围 → A 档落地,B 档作可选层,C/D 完全否决。**

| 档 | 内容 | 结论 |
|---|---|---|
| **A** 纯房间盒 | 矩形房间 + 门/窗 + 编号圆圈 + 图例 | ✅ **先落地跑通** |
| **B** + 家具图标 | 沙发/床/桌/书架/壁炉/楼梯… | ✅ **可选层**,只在功能二用 |
| **C** + 地板材质与阴影 | `<pattern>` 填充、墙体加厚、投影 | ❌ **完全否决** |
| **D** + 手绘感抖动线条 | 直线拆段加随机偏移、双重描边 | ❌ **完全否决** |

**否决理由留档,防止以后重开:** C 的成本陡升而收益走平——地板材质不影响任何跑团决策,
它是美术需求不是功能需求。D 虽然最贴阿卡姆观感,但随机性会让同一份 DSL 每次渲染出不同
SVG,**diff 全红**,必须额外引入 seed 机制才能复现,且每改一次都要肉眼验收。两者都被
否决,渲染器因此是**纯确定性的**——同样输入必然同样输出,这条是本计划的硬约束。

**3. 是否值得做 → 值得,而且是两个功能不是一个。** 见下。

## 方案:两个功能,共用一个渲染器

**这不是同一张图的两种画法,是两件不同的产物,给不同的人看。**

| | **功能一:场景定位图** | **功能二:可互动场景图** |
|---|---|---|
| 给谁 | **KP**,跑团时自己看 | **PL**,摆到桌上一起看 |
| 用途 | 快速定位、判断连通与视线 | 具体场景里和玩家互动 |
| 档位 | **A** | **A + B(家具层)** |
| 模型每次写(10 间房) | ~15 行 JSON | ~50–70 行 JSON,且要先读房间描述再判断家具布局 |
| 预估 token | ~400 | ~1500–2000,**约 3–5×** |
| 何时生成 | 场景生成时可顺手带上 | **必须 KP 主动要**,不自动生成 |
| 剧透 | 可含 KEEPER ONLY(暗门、秘密) | **必须 player-safe** |
| 落盘 | 场景 md 的 Map 小节 | `campaigns/<slug>/handouts/` + 渲染的 svg |

**成本必须告知。** 功能二比功能一贵 3–5 倍,而且贵在模型侧(要为每间房挑家具、判断朝向
与是否越墙),不是渲染侧。**生成前先把预估成本说给 KP 并等确认**——这条要写进 spec,
不是写进本计划就算数。上面的倍数是**估算,阶段 2 首次落地时实测回填**。

**剧透是功能二的硬门槛。** 一张递给玩家的图就是 player-facing prop,受 `core/11-review.md`
的剧透审查管:暗门、KEEPER ONLY 的房间用途、编号对应的秘密都不能出现在玩家版上。
同一份 DSL 要能渲两版(`--player` 开关过滤掉标了 `secret: true` 的元素),**不是让模型
写两份 DSL**——写两份必然不同步。

## 阶段 0 — 还债:mermaid 惯例落地(零阻塞,先做)

**现存 bug:两个进 bundle 的生产文件把本计划当规范引用,但规范不存在。**

```
templates/cult.md:42                  → "Build with the mermaid faction-map convention
                                         (update_plan/2026-08-02-low-cost-maps.md)"
reference/craft/cult-design-zh.md:68  → "与 P5 的 mermaid 势力图工具合流"
```

`update_plan/` **不进 bundle**,所以走 ChatGPT 链路的 KP 被指去查一份他拿不到、
且内容也不存在的文件。这是 P1 完结时留下的欠账。

- [x] 新建 `reference/craft/diagram-conventions-zh.md`(87 行,**已进 bundle**):
      §一 通用规则 / §二 势力图 / §三 场景网 / §四 区域关系图 / §五 不该画图的情况。
      **每条连线必须带标签**——写清连接二者的是什么(债务、血缘、共同的秘密、货物往来),
      孤立的圈没有用
- [x] `templates/cult.md` 与 `reference/craft/cult-design-zh.md` 两处引用改指向它
- [x] `core/03`(Output 加一条:何时附势力图/区域图)与 `core/04`(三线索规则加一条:
      画成场景网,**必到场景入边少于 3 条即卡点**)各接一行
- [x] `reference/craft/README.md` 加条目行,并写明本目录**有两类条目**:提炼稿(受
      「取手法不取文字」管)与惯例规范(kit 自订、无源材料)。`reference/README.md` 同步登记
- [x] 重跑 `build-reference-index.py` → **no problems**,新文件 8 refs / 3 declared
      consumers,不孤儿(当时还顺手重跑了 `build-bundle.sh`;该脚本已随 **P13** 删除)
- [x] `CHANGELOG.md` 追加(更新内容 + 修复问题各一条,并入当天已有的 2026-08-04 条目)

> **阶段 0 于 2026-08-04 完成,随 `13821ac` 提交**(该 commit 是当天六股工作的合并落地,
> 阶段 0 的三个文件——`craft/diagram-conventions-zh.md` 新建、`templates/cult.md` 与
> `craft/cult-design-zh.md` 两处还债——都在其中)。hash 已于 2026-08-06 回填到本文件头与
> `update_plan/README.md` 状态表。

## 阶段 1 — 功能一:场景定位图(A 档,已完成 2026-08-07)

- [x] **原型先行**:找一份**有实际内容的地点稿 + 一个场景稿**各做一张,**看过 SVG 再
      定稿惯例**。原型不合格就回头改 DSL,不要先写模板。
      (原定用 `campaigns/beidaihe-winter/` 的海滨与夜数场景当样本,**该战役已于
      2026-08-04 删除**;现在仓库里没有在跑的战役,样本得先写一份或改用
      `reference/_source/arkham-maps/` 的 `interior-*` 当参照——**那批图现已入库**,
      不再是"本地若有"。)
      **实际做法(2026-08-07,Keeper 拍板):** 样本战役临时用完即弃,不进 `campaigns/`、
      不提交——在 scratchpad 写了一份 100% 原创的两层楼样本(「灰烬庄园」一楼,含矩形
      房间、共享墙、门、窗、楼梯、圆形塔楼、引线标注全部要素),跑通渲染器后连同 JSON
      一起丢弃,仓库里没有留下任何痕迹。模型本身没有可视化能力,验收分两层:①逐元素
      核对渲染出的 SVG 坐标与预期几何(墙的粗细分类、门缺口区间、窗虚线位置等,详见
      渲染器 self-check 记录);② 发布成 Artifact 页面请 Keeper 目测,确认门楔形/虚线窗/
      内外墙对比在视觉上读得出来——**Keeper 确认「达标,按此定稿」**。事后又对照
      `reference/_source/arkham-maps/interior-1.jpeg` 复核一遍:门的楔形角度、楼梯斜线
      阴影+箭头、圆形房间、引线标注(「上锁的门 **?**」)全部对得上;**唯一没有复刻的
      是原图部分内墙用的"撕纸边缘"锯齿线**——判定这属于 C/D 档已否决的手绘感装饰
      (「墙体厚度建模+投影+材质」「手绘感抖动线条」两条否决理由都适用),不追加
- [x] `scripts/render-map.py` — DSL→SVG,**无外部依赖,stdlib 即可**;纯确定性
      (同一份 DSL 渲染两次输出字节完全一致,已用 diff 核过)
- [x] DSL 定型:房间 `{id, name, x, y, w, h, doors[], windows[]}`;门窗用「哪条边 + 边上位置」
      表达,不用绝对坐标(模型算绝对坐标必错)
- [x] **渲染要素照 `interior-1/2/4` 的体例**(下列每一条都有样本可对):
      - 墙:**粗实心黑,外墙粗内墙细**。这只是加大 stroke-width,**免费**
        ——别和 C 档否决掉的「墙体厚度建模 + 投影 + 材质」混为一谈
      - 门:**墙上开口 + 一个 ∧ 形楔子**(不是弧线)
      - 窗:**外墙外侧的短虚线段**
      - 楼梯:**横线阴影矩形 + 箭头 + 「向上/向下」**
      - 圆形塔楼直接画圆
      - **房间名写在房间里**,居中一行。中文短,`text-anchor="middle"` 就够
        ——原来"只画编号不写名字"那条已按样本推翻,编号圆圈留给城市尺度
      - 引线标注(箭头 + 文字):`interior-1` 的「上锁的门 **?**」「钉着木板的窗户和约束装置」
        就是这个。**那个 `?` 本身就是 KP-only 的秘密标记**,天然对应阶段 2 的 `--player` 开关
        (阶段 1 只落 schema 的 `secret` 字段与斜体渲染,不做过滤——过滤是阶段 2 的活)
- [x] `templates/location.md` / `templates/scene.md` 增加**可选** "Map" 一节(DSL JSON 块)
- [x] `core/03-build-world.md` / `core/09-description.md` 各加一行:何时附图、用哪种
- [x] 多层建筑**每层一张图纵向堆叠**,标题「一楼/二楼/三楼/地下室」(样本就是这么排的),
      **不做楼层叠加**(那是 C 档的复杂度,已否决)。**实现口径**:一份 DSL = 一层,
      多层建筑写多份 DSL 文件分别渲染,在 location.md 里按楼层顺序连续放多个 Map 小节
      ——「纵向堆叠」发生在文档阅读顺序,不要求渲染器把多层拼进同一张 SVG

## 阶段 2 — 功能二:可互动场景图(B 家具层,已完成 2026-08-08)

- [x] **不建图标库。家具 = 几何图元 + 标签文字。** 依据是 `interior-4`(密大图书馆)——那张图
      家具密度很高,但**没有一个是图标**:书架是细长矩形、阅览桌是圆、阶梯教室座位是
      同心弧加点、讲台办公桌是矩形。**形状只负责占位,含义全在标签里**(报纸/期刊合订本/
      旧书/工程学/冶金学/地理学)。

      > **这条推翻了本计划初稿的「图标库封顶 12 个」。** 初稿担心的"永远增长的美术资产"
      > 因此根本不存在,B 档的渲染器规模从 ~350 行掉到 ~200 行(图标 path 常量本来占一半),
      > 模型写的也从"挑图标编号"变成"写标签"——后者才是它擅长的事。
- [x] 家具 DSL 用图元不用类型枚举,坐标**相对所在房间**,不是全图绝对坐标:
      ```json
      "furniture": [{"s": "rect",   "x": 1, "y": 0.5, "w": 2, "h": 0.4, "label": "书架"},
                    {"s": "circle", "x": 3, "y": 2,   "r": 0.6,          "label": "阅览桌"}]
      ```
- [x] 渲染器加**越界与重叠检查**:家具画到墙外或互相压住时报错,不静默出图
      (这是 B 档最容易翻车的地方)。**实际落地**:`validate_furniture()` 对每间房各自的
      家具列表做两件事——边界框是否超出该房间自己的 `w`/`h`(圆形房间退化成外接方框
      近似),以及同房间内两两包围盒是否重叠;任一命中就 `ValueError` 非零退出,不写文件。
      已用两个 scratchpad 样本(超界柜子 / 桌子与花瓶重叠)实测,两者都精确报错并退出码 1
- [x] `--player` 开关:过滤 `secret: true` 的房间用途、门、家具与编号,出 player-safe 版。
      **实际落地口径**:秘密门→那段墙渲染成实墙(缺口消失,不只是锁着);秘密房间/图元→
      `player_name`/`player_label` 顶替(给个障眼说法),没给就留空(房间形状仍画,不是
      整间房消失);秘密家具→有 `player_label` 就顶替,没有就整件家具从 player 版里消失
      (默认值是"完全藏起来",家且没有覆盖场景就更接近现实里"藏起来的东西");秘密引线
      标注→player 版直接丢弃(KP 版仍斜体照旧显示)。**"编号"(城区图的数字圆圈)未落地**
      ——那是本渲染器还没做的城区尺度图种,不是本阶段漏做,docstring 里点名说清楚了范围。
      已用一份两室样本(书房+密室,密室整间是秘密、门是暗门、里面一件秘密祭坛家具)实测
      KP/player 两版逐元素核对全部正确
- [x] **成本告知**写进 spec:`core/09-description.md` → Output(功能一/二共用的地图落点)
      ——生成功能二之前,把预估 token 成本说给 KP 并等确认。`core/10-create-handout.md`
      的「Form fits function」补一句指路,产物仍算一种 handout,走同一套 player-safe 纪律
- [ ] 首次落地时**实测**功能一/功能二的实际 token 差,回填本文件的 3–5× 估算——**留给
      第一次真实生成时回填**,本轮只有 scratchpad 合成样本,没有真实场景可比对成本,
      不编造数字
- [x] 产物落 `campaigns/<slug>/handouts/`,受 `core/11-review.md` 剧透审查——
      `core/11-review.md` 「Every player-facing file (`handouts/`, boxed text) is
      spoiler-clean」这条本就通用覆盖 `handouts/` 目录下任何文件,不必新增专属审查项

## 阶段 2b — 室外站点图(已完成 2026-08-08:复用同一渲染器,未新增图种)

庄园/野外场景**要出图**,但**不为它单写渲染器**。把 `site-crowninshield-manor` 拆开看,
它需要的东西 A/B 档已经在做了——**"没有墙的画布"不是新功能,就是房间数组为空、只有图元层**:

| 庄园图上的东西 | 用什么画 | 新功能? |
|---|---|---|
| 森林 / 果园 / 花园(区域) | 带标签的大矩形或圆 | ❌ B 档图元 |
| 公馆 / 车库 / 储藏室 | 小实心矩形 + 标签 | ❌ B 档图元 |
| 「大理石圆形建筑」 | 小圆 + 标签 | ❌ B 档图元 |
| 引线标注 | 箭头 + 文字 | ❌ 阶段 1 已有 |
| **小路 / 溪流** | **折线** | ✅ 唯一新图元 |
| 指北针 | 角落固定图形 | ✅ 城区图也有 |

- [x] 折线图元:`{"s": "path", "pts": [[x,y],…], "label": "小路,步行 5 分钟",
      "style": "solid|dashed"}`。**约 15 行**。**实际落地**:顶层新增 `paths` 数组,
      `render_paths()` 输出一条 `<polyline>` + 中点标签,`style: "dashed"` 时加
      `stroke-dasharray`;`max_x`/`max_y` 的版面计算已把路径点纳入
- [x] 指北针图元,固定画在角落。**约 5 行**。**实际落地**:顶层 `compass: true` 触发
      `render_compass()`,固定画在画布右上角(像素坐标,不参与网格单位系统——它是页面
      固定装置,不是地点本身的一部分)
- [x] **不做纹理填充**(森林点簇、果园网格、葡萄藤横纹)——浅灰形状 + 标签文字即可,
      与 C 档否决保持一致
- [x] **克制规则:站点图目标 5–9 个元素,超过就该拆两张或改用文字。** 依据是书里那张
      农场站点图——只有「高地 / 虚线区域 / 毁坏的农场房屋 / 井」四个元素,照样可用。
      **这条是给模型写 DSL 时看的克制规则,脚本本身不做硬校验**——落进了
      `scripts/render-map.py` 的 docstring 与 `templates/location.md` 的 Map 小节
- [x] 图上表达不了的东西(从公馆能否看到森林、跑过去多久)**写进场景正文,不让图背责任**
      ——这正是阿卡姆的做法:城区图上只有编号,细节全在正文。**实际落地**:B 档的
      `furniture` 房间内坐标系与站点图的 `features` 顶层坐标系共用同一套 rect/circle+label
      图元(`render_shape_layer()` 一份实现服务两处调用点),站点图的地物同样支持
      `secret`/`player_label`,与家具层的 player 过滤规则保持一致——没有另起一套写法。
      已用一份庭院样本(果园/秘密车库/大理石亭 + 一条碎石小路 + 指北针)实测 KP/player
      两版逐元素核对全部正确

**为什么不另造一套文字格式:** 省的不只是那 20 行代码,是**不用再教模型第三种写法**。
站点图和室内图共用同一份 DSL 语法,模型学一次就够。

## 阶段 3 — 降级路径(2026-08-07 拍板:砍掉,不做)

> **P15 阶段 0 P13 余波扫描(2026-08-06)结论,只报不改:** 本节整节假设存在一条
> "无文件系统的 ChatGPT 网页链路",而 `core/00-how-to-run.md` → "If you cannot write files"
> 与 Layout 一节已写死 kit 由**有文件系统的 agent 就地读**(Claude Code / codex /
> gemini CLI)。该假设若已不成立,本阶段(连同下面"链路差异说明要写进 `core/00`"那句)
> 可能整块不必做——**这同时决定 P10 建筑卡那张的存废理由**(P10 修订 B:"砍掉它
> ChatGPT 链路就彻底没有室内地图")。**两者要一起确认,开工前先答这个,不要分两次答。**

**2026-08-07 拍板:两者一起答,结论是砍掉本阶段。** `core/00-how-to-run.md` 现在写死
"kit 由有文件系统的 agent 就地读……没有构建步骤,也没有单文件导出"(P13 退役 bundle 时的
后续措辞),全文再没有任何地方提"ChatGPT 网页链路"——那个使用场景已经不存在,不是"暂时
没写清楚"。**本阶段整节不做**,下面 checkbox 不再执行。

**这也回答了 P10 修订 B 的存废理由。** `diagram-conventions-zh.md` §六 建筑卡已经在
P10 阶段 2 落地(2026-08-07,`7731a01`)——**不追溯撤销**,它现在是"跑不了
`scripts/render-map.py` 时的通用兜底格式"(没有 Python、Keeper 不想为一张草图开一次渲染),
这个理由不依赖 ChatGPT 链路是否存在,继续成立;只是**不必再为它扩展内容**,§六 已经是
终态。

- [x] ~~无文件系统的环境(ChatGPT 网页)跑不了渲染器……~~ —— **不做**,理由见上。

## 收尾

- [x] ~~动过 `core/` / `templates/` / `reference/` → 重跑 `scripts/build-bundle.sh`,同 commit~~
      —— **该硬约定已作废,脚本本身也已删除**(2026-08-04:先是 `dist/` 退出版本控制,
      随后 **P13** 把整条 bundle 链路退役)。kit 现在没有构建步骤,见
      `reference/README.md` → 原创 vs 第三方
- [x] `render-map.py` 与 `render-investigator.py` 一样,本机可直接跑验证(见备忘)——
      2026-08-08 用 `python 3.14.5` 跑通四个 scratchpad 样本(带家具+暗门+秘密房间的
      室内样本、越界家具、重叠家具、带 features/paths/compass 的庭院样本),KP/player
      两版逐元素坐标核对全部正确,越界与重叠两个坏样本都精确报错并非零退出;样本验证完
      即弃,不进 `campaigns/`、不提交
- [x] `CHANGELOG.md` 追加(面向 Keeper:现在场景能带图了,以及玩家版图要主动要)
- [x] 走完 `update_plan/README.md` 的完结清单

## 备忘

- 拆分时(2026-08-02)repo 内**没有**任何原型产物:无 `scripts/render-map.py`,
  campaigns 下无示例图。2026-08-04 复查仍然如此
- ~~环境限制:本机无 Python 解释器可用~~ —— **2026-08-02 更正**:本机有
  `python 3.14.5`(`C:\Users\User\.local\bin\python.exe`),P8 已实跑
  `render-investigator.py` 证实。`render-map.py` **写完可以直接跑**,不必靠人工复核代码
- **阿卡姆地图样本存放在 `reference/_source/arkham-maps/`(Keeper 已筛到 20 张并按内容
  重命名)**——第三方图片,当**视觉参照**用,**不是拿来转描的**。
  ~~已 gitignore,永不入库~~ → **2026-08-04 晚已入库**(理由:计划里「渲染要素照
  `interior-1/2/4` 的体例」这句,料不在库里就没法复核),同时按 ASCII 硬约定改了文件名,
  新旧对照见 `reference/_source/README.md`。
  写渲染器时**直接对着 `interior-1` `interior-2` `interior-4` 三张调**,阶段 1 的每条渲染
  要素都能在这三张上找到样本;站点图对 `site-crowninshield-manor` 与 `exterior-1/2`。
  要看更多可从 `reference/_source/阿卡姆.docx` 再抽(**那份 12 MB 原件仍是本地件**,
  被 `*.docx` 挡着,clone 下来没有)
- 三个原待拍板问题的定案过程与否决理由见上面「已定案」一节。**C/D 是完全否决不是暂缓**,
  以后想重开要先推翻那里写的理由

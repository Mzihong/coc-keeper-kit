# Update Plan — 反派强度预算(待讨论)

> 日期:2026-08-02
> 状态:**待讨论** —— 本文只摆事实、列选项、提问题,不含已定案的执行清单
> 起因:`2026-08-02-cult-doc-integration.md` 第三章 C 项。通读《克苏鲁教团》第三章后
> 发现一整组"反派该多强"的惯例数值,kit 现有 `core/` 与 `reference/rules/` 都没有对应
> 位置,且第四章的数值校验依赖它

## 问题陈述

kit 现在能造反派(`core/06-create-npc.md`、`core/07-create-monster.md`、
`core/13-create-investigator.md`),但**没有任何地方回答"这个反派该多强"**。

后果有三:
1. `core/11-review.md` 能查数值**自洽**(算术、派生值),查不出数值**过强**——
   一个技能全 90% 的邪教首领可以毫发无伤地通过现有审计
2. 教团文档第四章有 11 个邪教徒原型 + 数个精英 NPC 全带数值,计划写"先过 `core/02` 校验",
   但 `02` 没有强度标尺,这条校验目前是空转
3. conventions-gaps §7 刚加的 "Scaling" 侧栏(按人数增减对手数量)调的是**数量**,
   没有调**单体强度**的依据

## 已有素材:第三章给出的惯例数值

原样记录在此供讨论,**落盘时一律重写/重建,不照抄**(红线同教团计划)。

**技能点总预算带**(源文档称之为对邪教首领的标准建议):

| 预算 | 定位 |
|---|---|
| 300–400 | 弱,约等于一个平均水平的调查员 |
| 400–500 | 约等于一个强于平均的调查员 |
| 600–700 | 中等到强大 |
| 800–1000 | 非常强大 |

**技能点分配的四种方法:** ①标准池 `EDU×4 + INT×2 + 200`;②直接指定(快但易膨胀);
③抄现成原型改;④用预设数组。预设数组五组:

- A / B / C:6 个关键技能,总计 350 / 390 / 425 点
- D / E:12 个关键技能,总计各 700 点

**属性预设数组四组**(分配到 STR/CON/SIZ/DEX/INT/APP/POW/EDU):
A 平均 / B 高于平均 / C 强大 / D 长者(体能衰退、精神不减,高值给 POW/INT/EDU)。
配套两条护栏:属性 **80+ 会偏离创角的现实性与公平性**;施法者 **POW ≥ 70**。

**法术数量按资历:** 年轻 `1D4+1` / 标准 `1D6+1D4+2` / 成熟 `3D6+4` / 古老 `4D6+10`。

**SAN 惯例:** 邪教首领几乎都是 0;未走完堕落之路的可留 10–30。
普通邪教徒同样多为 0;守秘人想留救赎可能性时 10–35。
**明确规则:NPC 的 SAN 不掷,直接定。**

**技能上限的判断法**(比数字更有用的一条):技能值要与角色概念相称,且
"90% 步枪的物理学教授"既不合概念、**对调查员也过于致命**——上限由**致命性**倒推,
不由角色背景正推。

## 待讨论 1 — 归属:放哪儿?

| 选项 | 优点 | 缺点 |
|---|---|---|
| A. 新建 `reference/rules/antagonist-budget.md` | 与 `sanity.md`/`chases.md`/`character-creation.md` 同级,`core/02` 登记一行即可;主题独立好找 | `reference/rules/` 再添一份,速查文件数量继续涨 |
| B. 追加进 `reference/rules/character-creation.md` | 造调查员与造反派共用一套点数概念,同一份文件里对照最直观 | 该文件现在是**玩家侧**车卡速查;混入反派预算会模糊定位 |
| C. 直接写进 `core/02-rules-reference.md` | 强制"写数值前必读"的路径最短 | `02` 是规则查询 spec,不是数值惯例仓库;与其他速查的分工被破坏 |

倾向 **A**,但 B 的理由不弱——**请 Keeper 定**。

## 待讨论 2 — 泛化:只管人类反派,还是全部?

源材料是**邪教首领专用**的。kit 里"反派"至少三类:

- **人类 NPC**(`core/06`)—— 直接适用
- **精英/满卡 NPC**(`core/13`,复用 investigator schema)—— 直接适用,且预算带正好
  与"平均调查员"对标,这是最有价值的接线点
- **怪物/神话实体**(`core/07`)—— **不适用**。怪物的强度尺度是 SAN 损失、护甲、
  攻击骰,不是技能点池。硬套会得出荒谬结果

问题:
- 文件是叫 `antagonist-budget`(暗示涵盖怪物)还是 `human-antagonist-budget` / `npc-power-budget`?
- `core/07-create-monster.md` 要不要也接一句"怪物不走本预算,走 X"?若要,X 是什么——
  kit 现在有没有怪物强度的对标物?(需查 `07` 与 `reference/bestiary/` 现状)

## 待讨论 3 — 预算带的基准需要验证

预算带说"300–400 ≈ 平均调查员"。这个对标**必须在 7e 规则下自验**,不能直接采信论坛数字:

- 7e 调查员职业技能点 = `EDU×4`(或职业指定的属性组合),兴趣点 = `INT×2`
- 以 EDU 70 / INT 70 计:280 + 140 = **420 点**(不含技能基础值)
- 那么"300–400 = 平均调查员"偏低一档,"400–500 = 强于平均"其实才是平均

→ 待办:拿 `reference/rules/character-creation.md` 已有的车卡速查实算一遍,
**用算出来的数字重建预算带**,而不是抄源文档的。这条同时是红线上的正确做法。

## 待讨论 4 — 与人数缩放(conventions-gaps §7)的关系

§7 的 Scaling 侧栏按 party size 调**对手数量、线索冗余、SAN 总压力**。
单体强度预算与之是什么关系?

- 方案甲:正交。人数变化只调数量,单体强度锁定在剧本设定的那一档
- 方案乙:联动。给一条"每偏离基准人数 ±N 人,主要反派预算带升/降一档"的换算
- 方案丙:只给判断口径不给公式——"低于基准人数时优先降数量,数量已是 1 时才降强度"

倾向**丙**:公式化容易产生假精度,而 CoC 的致命性本就不靠精细平衡。**请 Keeper 定**。

## 待讨论 5 — 要不要带"致命性倒推"这条口径

上面记的那条(上限由对调查员的致命性倒推,不由角色背景正推)我认为是本章**最有价值的
一句**,比任何数组都通用。若采纳,它应该:

- 进 `core/11-review.md` 审计清单:"反派技能值是否可由单次成功检定造成不可逆后果?"
- 与 campaign `CLAUDE.md` 的 **lethality** 字段挂钩(该字段已存在于 intake)

这条会把本计划从"数值速查"升格为"审计能力",范围变大——**是否纳入本轮,请 Keeper 定**。

## 需要先查清的事(执行前)

- [ ] `core/02-rules-reference.md` 现有 cheat-sheets 清单结构,确认新文件如何登记
- [ ] `reference/rules/character-creation.md` 是否已含职业点/兴趣点公式(用于待讨论 3 自验)
- [ ] `core/07-create-monster.md` 现在有没有强度对标,决定待讨论 2 的答案
- [ ] `core/11-review.md` 现有清单里数值检查到什么粒度

## 讨论定案后再写执行清单

以上五项定了再补 `- [ ]` 执行项。**在教团计划第四章开工前必须至少定完 1、2、3**——
第四章的 11 个邪教徒原型要靠它校验。

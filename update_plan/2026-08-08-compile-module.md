# Update Plan — 编译成模组文本:一幕一份,正文线性、网状收进表

> 日期:2026-08-08
> 状态:进行中(阶段 1–3 已完成,未提交;阶段 4 条件执行、阶段 5 阻塞)
> 来源:Keeper 2026-08-08 会话——「战役的基本拼图有了,接下来要组成类似官方模组一样的
> 具有可读性的文本文件,以线性的方式把所有创造出的拼图整合在一起(详情参考
> `reference/sourcebooks/keeper-rulebook-7e-zh.md` 第十五章)」;同日拍板
> **「正文线性、网状收进表」**
> 前置:无(原软依赖 P17 已随 `npcs/roster.md` 落地解除)

## 目标

给 kit 加一个**新产物**:把一幕的全部素材重排成一份**能从头读到尾**的模组文本,
体例照规则书第十五章。

**这不是把 `world/` 和 `scenes/` 串起来。** 现有的
[`templates/scenario.md`](../templates/scenario.md) 是**设计稿**——线索矩阵、场景节点、
三种结局,给作者收敛用;模组文本是**阅读稿**,给桌前逐页翻用。同一套事实的两种排布,
互不取代。

## 这不是 `dist/bundle.md` 重来(先把这条说清,否则会被硬约定误伤)

P13 退役了单文件 bundle,`WORKLOG.md` 硬约定里写着 **"没有构建产物,也没有构建步骤"**。
模组文本和它有四处本质区别:

| | `dist/bundle.md`(已退役) | 模组文本(本计划) |
|---|---|---|
| 范围 | 全仓一份 | **一幕一份** |
| 读者 | 模型(单文件上传链路) | **Keeper,桌前翻** |
| 生成方式 | 脚本机械拼接 | **模型转写**,有真实信息重排 |
| 信息增益 | 零(纯连接) | 时间线改写、数值内联、过渡文字 |

退役 bundle 的理由是"零增益 × 必然漂移"。本计划保留漂移这一半的顾虑
(见「定案 ②」),不保留"因此不做"的结论。

## 体例依据

`reference/sourcebooks/keeper-rulebook-7e-zh.md` 第十五章《古茂密林之中》
(**13186–14729 行**)。拆出来的顺序:

1. 标题 + 一句话前提
2. **介绍**(给 KP:年代地点可替换、本模组重行动轻调查、**最吃哪些技能**、开跑前先读哪些条目)
3. **背景**(真相,从最远推到近)
4. **近期事件**(把真相推到开局那一刻)
5. **登场人物简介**(一行一人,含底牌)
6. **时间线**(第零日～第三日,**每天按阵营分行**,末句是"若不阻止会发生什么")
7. **调查员导入**(动机甲乙丙丁,**可当讲义随机发**,每条后跟一段"守秘人笔记")
8. **场景章节**(按日/地点线性排;每节 = 朗读框 → 有什么/能问到什么 → 检定与失败 → NPC 反应 → 出口)
9. **数值**(首次出场处内联)
10. **结局与奖励**
11. **附录:讲义、地图**

**关键读法:官方的"线性"是阅读顺序线性,不是游玩顺序线性。** 它没有把三线索的网压扁成
一条路——它把网折进了两个坐标(时间 × 地点),网状信息提前抽成第 6 节那张时间线表。
本计划照此办理。

## 定案(2026-08-08)

- ① **编译单位 = 一幕**,不是整个战役。开放长团(如 `beyond-the-treeline`)对应官方
  大战役书的形态:**战役总论 + 每章一模组 + 共享附录**,不是一本三百页的线性文本。
- ② **源文件权威,模组文本是派生物。** 顶部带 source manifest(源文件清单 + 编译日期)。
  改事实回源文件,重编译。
- ③ **时机 = 这一幕跑完之后。** 那时临场即兴已经进了 `canon-log.md`,模组文本是**整理稿**
  而不是预言稿,它的价值(复用、分享、给别的 KP 跑)本来就在事后。
  **跑之前要的是备课稿,那是 [`templates/session-prep.md`](../templates/session-prep.md)
  的活**——两者边界必须写死,否则会互相吞。
- ④ **第一步永远是 gap report**,不是直接编译。缺什么、缺了影不影响读,KP 挑着补。
  这是 P17「预览—确认—展开」在本产物上的实例。
- ⑤ **不写编译脚本。** 时间线转写、过渡文字、数值内联都不是拼接能做的事。脚本只可能
  做一件事:比对 manifest 检查源文件是否已改(阶段 4,条件执行)。

## 阶段 1 — `templates/module.md`

- [x] 章节骨架照上面 11 节,英文标题(硬约定 3:模板标题英文,内容按战役输出语言)。
- [x] 内含**来源映射表**,一列"这一节从哪来":

  | 模组章节 | 来源 |
  |---|---|
  | 标题 + 一句话前提 | `overview.md` 首行 |
  | 介绍 | `CLAUDE.md`(era/tone/lethality/party/content lines)+ 本幕最吃的技能 |
  | 背景 | scenario 的 truth + `world/timeline.md` 隐藏轨 |
  | 近期事件 | `world/event-clock.md` 的 0 号刻度 |
  | 登场人物简介 | `npcs/roster.md`(P17)+ 成卡文件首行 |
  | 时间线 | `event-clock.md` 的 doom track,**按阵营分行改写** |
  | 调查员导入 | hooks + `investigators/` |
  | 如何跑这一幕 | 线索矩阵 + 场景出入口表(**kit 自加,官方没有**) |
  | 场景正文 | `scenes/` + `world/` 地点 + `puzzles/` |
  | 数值 | 成卡 + `reference/bestiary/` |
  | 结局与奖励 | scenario 三结局 + SAN 奖励 |
  | 附录 | `handouts/`、`world/*.svg` |

- [x] **「如何跑这一幕」是本模板唯一比官方多出来的一节**,而且是拍板"网状收进表"的落点:
      正文之后只管线性,所有网状信息压在这一节。
- [x] 顶部 source manifest 块的格式(源文件路径 + 编译日期 + 一句"改事实回源文件")。

## 阶段 2 — `core/16-compile-module.md`

- [x] **步骤 1:gap report。** 列出这一幕缺哪些块(哪几个 NPC 还是 `stub`、哪几场没朗读框、
      哪条线索没落点),每条标**缺了影不影响读**。KP 挑着补,补完再编译。
      读 `npcs/roster.md` 的状态列判断 stub/成卡。
- [x] **步骤 2:转写规则**(逐条写死,这些是编译真正在干的活):
      - **时间线**:kit 的 clock 是"阶段",官方是"第 N 日 × 各方在干什么"。
        **这是编译时最大的一块转写工作,不是搬运。**
      - **数值内联**:NPC/怪物的 stat block 放在**首次出场处**(桌前不翻页),附录再汇总一份。
      - **讲义编号**:统一 `H1`、`H2`…,正文用引用块提示,原件在附录。
      - **表/里两层**:campaign `CLAUDE.md` 声明了双层规约时保持——表层进朗读框,里层进紧跟
        的 `> **KEEPER ONLY**` 注,**表层里仍然不许出现里层任何一个词**。写成通用规则,不点名
        具体战役(`core/` 规范里不绑定单一战役)。
- [x] **步骤 3:硬规则——编译不许新造事实。** 转写过程中发现缺口,回源文件补,再重编译;
      **不许在模组文本里就地编一句圆过去**。这条已写进 `core/16` 正文,`core/11-review.md`
      的接线留给阶段 3。
- [x] **单文件上限**:超过约 1200 行拆 `-part2`。理由——长团单是 `world/` 就可能 1000+ 行,
      不设上限编译产物必然读不动。
- [x] 输出路径:
      ```
      campaigns/<slug>/module/
        00-campaign-primer.md    ← 各幕共享:世界观、转译词表、名录、地图索引
        <arc>-<slug>.md          ← 一幕一份
        appendix-handouts.md     ← 讲义统一编号
      ```
- [x] **与 `session-prep.md` 的边界**各写一句在两份文件里(定案 ③)——`templates/module.md`
      与 `templates/session-prep.md` 顶部各加一句互指。

## 阶段 3 — 接线

- [x] `core/00-how-to-run.md`:管线表加一步(在 canon update 之后,标明"一幕一次、可选")+
      路由表加一行("把这一幕整理成能读的模组""编译模组")。
- [x] `CLAUDE.md` / `GEMINI.md` / `AGENTS.md` **三份同步**加 `compile-module`(CLAUDE.md 是
      技能表,GEMINI.md/AGENTS.md 是路由表,体例分别照各自现有格式)。
- [x] `.claude/skills/compile-module/SKILL.md`(薄壳,只指 `core/16`)。
- [x] `campaigns/README.md` 目录说明(补第 5 步)+ `campaigns/_template-campaign/module/README.md`。
- [x] `core/11-review.md`:Blocking 新增"`module/` 文件每条事实必须溯源"审查项(步骤 3 的镜像,
      `n/a` 当本次审查不涉及 `module/`)。

## 阶段 4 — 漂移检查(条件执行)

**判据:先手工编译过一份真实的模组文本再决定要不要写脚本。** 没有真产物之前,
不知道漂移到底以什么形式发生。

- [ ] `scripts/check-module-drift.py`:读 manifest,比对源文件 mtime/哈希,提示该重编译。
- [ ] 不做的:自动重编译、自动合并。**方向永远是源文件 → 模组文本,单向。**

## 阶段 5 — 样张验证(阻塞)

- [ ] 用 `beyond-the-treeline` 第一幕实测一遍完整流程。
      **阻塞:该幕现在还不存在**——`world/event-clock.md` 未建、`npcs/` 全空、
      第一场 scenario 未设计([`overview.md:15`](../campaigns/beyond-the-treeline/overview.md)
      自己写着"尚未设计")。
- [ ] 实测后回填:真实行数、gap report 命中了几项、时间线转写花了多少工夫。
- [ ] **样张不合格就砍掉阶段 4**,并重新审阶段 2 的转写规则——宁可退回"只有模板没有 spec"。

## 阶段 6 — 收尾

- [ ] `CHANGELOG.md`(面向 Keeper:"跑完一幕后可以把它整理成一份能读的模组")
- [ ] 根 `README.md` 技能表 + 流程图
- [ ] `WORKLOG.md`(新增 `core/16`、新增战役子目录 `module/`,属于结构改动)
- [ ] `update_plan/README.md` 完结清单八项

## 风险与边界

1. **篇幅失控。** 官方一篇模组约 30 页;kit 的素材密度更高,不设上限会编出读不动的东西。
   挡法是阶段 2 的 1200 行上限 + 拆 part。
2. **转写退化成复述。** 最可能的失败形态是把 `world/` 的段落原样粘过来,没做任何重排——
   那样产物没有信息增益,就变成 bundle 了。判据:**时间线那一节必须是新写的**,
   照抄 clock 的阶段表就算不合格。
3. **泄底。** 模组文本**全部是 Keeper-facing**。玩家能拿到的只有附录讲义与 `--player`
   版地图([`world/README.md`](../campaigns/beyond-the-treeline/world/README.md) 已写明
   现有两张 svg 是守秘人版)。这条进 `core/11` 的 Safety & spoilers。
4. **和 `session-prep.md` 互吞。** 定案 ③ 已划线,但两者章节高度相似,落地时要各写一句
   边界,不能只在计划里写。

## 明确不做

- **整个战役编成单文件**——违反定案 ①,也是 bundle 的老路。
- **机器拼接脚本**(定案 ⑤)。
- **玩家版模组正文**。玩家拿的是讲义和地图,不是删过秘密的模组——"删过秘密的模组"是
  一个必然会漏的产物,不做。

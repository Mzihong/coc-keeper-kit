# update_plan/ — Status Tracker

改动计划的索引与执行指引。**执行任何计划前先读本文件**;完成一步就回来更新状态。

## 状态索引

只列**未完成**的计划。已完成并归档的计划不在本表出现,索引见
[Archived/README.md](Archived/README.md)。

| # | 计划 | 范围 | 状态 | 阻塞/等待 |
|---|---|---|---|---|
| P5 | [low-cost-maps](2026-08-02-low-cost-maps.md) | 低成本地图:mermaid 惯例(还债)+ DSL→SVG 渲染器,两个功能——KP 定位图(A 档)与 PL 可互动场景图(A+B 家具层) | 进行中(阶段 0 已完成) | **2026-08-04 三个待拍板问题已全部定案**(C/D 档完全否决);阶段 0 落地 `craft/diagram-conventions-zh.md` 并还清 mermaid 惯例那笔债,**hash 待提交后回填**;下一步阶段 1 原型 |
| P10 | [town-anatomy-from-arkham](2026-08-04-town-anatomy-from-arkham.md) | 阿卡姆资料 → `craft/town-anatomy-zh.md` 城镇解剖手法稿 + `tables/town-institutions.md` d20 机构表 + `craft/diagram-conventions-zh.md` **扩 §六 文字地图卡**(3 张原创样例内嵌) | 进行中(阶段 0 已完成,全部定案,已复评) | **无阻塞**。阶段 0 勘察 2026-08-04 完成(结论见计划内「勘察结果」);当日三个拍板问题全部定案(全文**不**归档——撞硬约定;d20 表**做**;地图卡 3 张、内容全原创)。**同日复评修订六处**(见计划文末「复评记录」):地图卡改为扩 `diagram-conventions-zh.md` §六 而非新建文件、并还掉该文件 §四 一处悬空术语;d20 表剔掉神话层 2 类;阶段 1 新增采料步、§三/§四 换定义。下一步阶段 1.0 采料 |
| P12 | [cheatsheet-self-sufficiency](2026-08-04-cheatsheet-self-sufficiency.md) | 速查表自足化:战斗/追逐/理智三份按规则书第六、七、八章重写(**现有版本有实证错误**)+ 新建恐惧症/躁狂症 d100 表与武器强度索引 + 规则书 `git rm --cached` 原地留档 + 其余四份补齐 | 待执行(**三问已于 2026-08-04 全部定案**) | **无阻塞**。① 转载规则的边界(`WORKLOG.md` 硬约定 5)改为「规则条文 vs 虚构散文」(规则可直接引用,散文仍只取手法);② bundle 整套退役 → P13 已完成归档,体积不再是约束;③ 恐惧症/躁狂症进 `tables/` d100 全表,武器另立强度预算索引。下一步阶段 0 出一节样例定体例;**阶段 1.0 先改约定文件再写表** |
| P14 | [scenario-diversity](2026-08-04-scenario-diversity.md) | 模组多样性:`scripts/roll.py` 真伪随机掷骰(不放回 + 跨战役查重)+ 补两张缺表(对抗场面 / 模组形状)+ locations 接到场景级;扩容降为条件执行 | 进行中(阶段 1 已完成并提交,148bb91) | **无阻塞**。阶段 1 已落地:`scripts/roll.py`(两维表、不放回、`--fresh`、非骰表排除名单、自检 + `--check-all`)+ ground rules 新增一条并三适配器同步 + `core/01/03/04/06`、`tables/README.md`、`core/11` 全部改完掷骰点措辞。下一步阶段 2(种子表口径不一致的 bug 修复 + locations 接到场景级) |
| P11 | [era-rule-packs](2026-08-04-era-rule-packs.md) | 年代开放:`rules/eras/` 差集包(书里覆盖的全建)+ 战役声明式加载 + 未覆盖年代的推导兜底 + review 串味校验 | 进行中(阶段 2b、3 已于 2026-08-05 完成,**尚未提交**) | **无阻塞**。阶段 2b 四条(`core/13` 加载顺序、busybodies 非 1920s 限定、路径 B 有写无读、五节体例中英节名)与阶段 3 三条(`core/11` 年代串味审查项、`eras/README.md` 自述同步、CHANGELOG 同步)均已完成——路径 B 那条按建议扩成了「Era 字段的定义收口到一处」,权威落在 `core/02` 而非 `eras/README.md`,顺带解掉 P15 问题 7。**只剩提交**:完结清单第 7 项要 commit hash 才能回填,归档留到提交后 |
| P15 | [core-spec-audit](2026-08-04-core-spec-audit.md) | `core/` 复查勘误:P13 余波的失效断言、`reference/external/` 死目录、**六份神格页只写不读**、`core/11` 缺镜像审查项;外加三条防复发的制度动作 | 待执行(**两个拍板点未定**;阶段 0 不需要拍板就能动) | **无阻塞,但有两处让路**:问题 2 的语义半边已并进 **P12 阶段 1.0**(同一句话,P12 是超集)、问题 7 已并进 **P11 阶段 2b**(与「路径 B 有写无读」是同一条缝);阶段 2 与 **P11 阶段 3** 撞 `core/11`,建议 P11 先做。九条问题归三类根因,**阶段 3 的三条制度动作才是主要产物** |
| P16 | [clue-engines](2026-08-04-clue-engines.md) | 线索引擎通用化(新表 `tables/clue-engines.md` d10,十台引擎各带一条独占结构属性)+ `cult-design-zh.md` §四 改口径 + `core/04` 六条三线索检验 + `templates/scenario.md` 加两列 + `core/05` 增 stall trigger + `core/11` 审查从「数三条」升级成三查 | 待执行(方向已于 2026-08-04 定案) | **无硬阻塞,但两处排序**:① 新表一出生就该接 `roll.py` → 软前置 **P14 阶段 1**(同 P10 → P14 那条实线);② 阶段 4 动 `core/11`,与 **P11 阶段 3**、**P15 阶段 2** 三方撞车,建议 P11 → P15 → P16。与 P14 是同族诊断(该掷的维度没有表),但多带一半治的是**卡关**不是多样性 |

**状态取值:** `待讨论(<待定什么>)` / `待执行` / `进行中(<当前所在步骤>)` / `阻塞(<等什么>)` /
`已完成(<commit>)`(完结后移除本表,见完结清单第 7 项)

`待讨论` 用于只摆事实、列选项、提问题,尚未产出 `- [ ]` 执行清单的计划——它没有条目可勾,
定案后先补执行清单再转 `待执行`。

条目级进度看各计划文件内的 `- [ ]` 复选框——本表只跟踪计划级状态,不重复条目。

## 依赖图

```mermaid
graph LR
    P5["P5 低成本地图<br/>(已定案,无阻塞)"]
    P10["P10 城镇解剖手法稿<br/>(已全部定案,可做到底)"]
    P11["P11 年代开放<br/>(无阻塞,可做到底)"]
    P12["P12 速查表自足化<br/>(已定案,可做到底)"]
    P14["P14 模组多样性<br/>(已定案,可做到底)"]
    P15["P15 core/ 复查勘误<br/>(两个拍板点未定)"]
    P16["P16 线索引擎 + 三线索检验<br/>(已定案,可做到底)"]

    P10 -. 阶段 2 镇级地图卡 = P5 阶段 3 的降级格式 .-> P5
    P12 -. 阶段 2 动 character-creation.md 前须扫 eras/ 差集 .-> P11
    P12 -. 都动 reference/tables/,别同批 commit .-> P14
    P10 == town-institutions 是高频表,该一出生就接 roll.py ==> P14
    P15 -. 问题 2 已并入 P12 阶段 1.0,等它 .-> P12
    P15 -. 问题 7 已并入 P11 2b;阶段 2 与 P11 阶段 3 撞 core/11 .-> P11
    P16 == clue-engines 是新骰表,该一出生就接 roll.py ==> P14
    P16 -. 阶段 4 与 P11 阶段 3、P15 阶段 2 三方撞 core/11 .-> P15
```

**P14 阶段 1(`scripts/roll.py`)已于 2026-08-05 落地并提交(148bb91)**,下面两条耦合描述的
前置条件——"roll.py 还没落地"——**已不成立**;保留这两段是因为道理没变:P10/P16 新写的表
仍然应该**一出生就调用 `python scripts/roll.py <新表>`**,而不是先手掷用几次再回头接线。

**P16 → P14 是第二条实线耦合(2026-08-04 新增),和 P10 → P14 同型。** 判据也一样:
新表若在 `roll.py` 落地前先被用起来,它会以"模型口头报点数"的方式被摸熟,之后要回头改接线。
**但 P16 比 P10 更吃这条**——它的表头写死「一次只掷 2–3 条通电,不许全开」,
而"不许全开"这件事**没有脚本就没有强制力**,是全计划最大失败模式(风险 1)的唯一挡法。
连带一条与 P10 相同的欠账:**P14 阶段 1.2「逐处改掷骰点的措辞」的清单里没有 P16 会新增的
那处 `core/04` 第 5 步掷骰点**——谁后做谁补。

**P10 → P14 是这批计划里唯一一条实线耦合(2026-08-04 新增)。** P14 诊断 ③ 是 kit 里唯一
系统想过「哪张表一个战役掷几次」的地方,结论是**高频表才会被摸熟、才需要不放回与跨战役
查重**。而 P10 新建的 `tables/town-institutions.md` 自己写着「掷 N 次填清单,N 随镇的规模
走」——**它一出生就在高频档**。两个计划各自都对,但若 P10 先落地而不接 `roll.py`,
这张表会先以"模型口头报点数"的方式被用起来,之后要回头改接线。
连带一条:**P14 阶段 1.2「逐处改掷骰点的措辞」的清单里没有 P10 会新增的那两处
`core/03` 掷骰点**——谁后做谁补。

P9(怪物强度标尺 + 索引层 + 神格铺设,三阶段)已于 2026-08-04 全部完成并归档,
见 [Archived/README.md](Archived/README.md)。

**2026-08-04 第二轮拍板后,P12 的三个问题全部定案,并因此分出了 P13、P14 两条新计划;
P13(bundle 退役)当日执行完毕并归档。** **P14 同日二次会话换了方向并全部定案,现在
五条计划都没有硬阻塞。**(P14 原本是唯一在等拍板的一条,理由是"要推翻 `tables/README.md`
一条成文约定"——换方向后不再需要推翻,那条约定原样保留。)
P5 的三个待拍板问题当日全部定案(C/D 档完全否决);P11 从来没有硬阻塞(目标是年代开放这项**能力**,不是预挑几个年代建包);P10 的
阶段 0 不需要拍板,而且拍板本身就需要勘察结果才答得了——**该阶段与三个拍板问题当日
全部完成,P10 现在也零阻塞**。三条计划彼此无依赖,可并行,唯一的软耦合是 P10 阶段 2 的
镇级地图卡格式正好是 P5 阶段 3 的降级输出格式。**2026-08-04 复评后这条耦合从"格式相同"
变成"同一个文件"**——地图卡直接扩进 P5 阶段 0 产出的 `craft/diagram-conventions-zh.md`
当 §六,P5 阶段 3 不必再跨计划找格式,顺带还掉该文件 §四 的一处悬空术语。
**仍然不构成硬依赖**:两边谁先做都行,先做的那个负责建 §六。

**P1(克苏鲁教团文档整合)已全部完成并归档**(阶段 0-2 见
[`cult-doc-integration`](Archived/2026-08-02-cult-doc-integration.md),阶段 3 收尾见
[`cult-doc-wrapup`](Archived/2026-08-02-cult-doc-wrapup.md),详情见
[Archived/README.md](Archived/README.md))。P9 不是任何硬前置——`core/07` 的占位句
已落盘。

## 建议执行顺序

> **2026-08-04 重排:改为按「损害」排,不再按「可动性/复杂度」排。**
> 旧版把 P10/P5 排在前面(理由是它们最容易动),却又在第 8、9 条的正文里写
> 「P12 优先级高于 P5/P10 剩下的部分」「P14 仅次于 P12」——**编号和正文互相打架,
> 接手会照编号做。** 现在编号即优先级;想按"哪个最好上手"挑活的,看下面的复杂度排序表。
> 已完成的历史条目移到本节末尾,不再占编号。

1. ~~**P11 阶段 2b → 阶段 3**(年代开放收尾)~~ —— **已于 2026-08-05 完成,尚未提交**。
   2b 第三条按建议扩成了「Era 字段的定义收口到一处」,权威收口在 `core/02`(不是
   `eras/README.md`),顺带解掉了 P15 问题 7(路径 C 无合法值、A/B 的 slug-vs-路径
   不一致)。详见 `2026-08-04-era-rule-packs.md` 阶段 2b/3 的勾选记录。
2. **P12 阶段 0 → 1**(速查表自足化)。**三问已定案,只等一节样例定体例。**
   它修的是**已经在牌桌上出错的数值**(`chases.md` 的载具 MOV 差一个数量级、
   `madness.md` 掷 4 号是另一件事),其余计划做的是新功能。
   **阶段 1.0(改转载规则的边界)必须先于阶段 1.1 写表**;它同时是 P15 问题 2 的超集,
   两边别各改一次。
   > **建议把阶段 1.2(`git rm --cached` 规则书)推到阶段 2 之后。**
   > 计划自己说阶段 2 的另外四份速查表还要读它,风险 2 也说退役后会留一批
   > `<!-- 待核 -->` 欠账。而"原地留档"是**本地**的——文件一出版本控制,
   > 仓库就再也 clone 不成一个能干阶段 2 的状态。且退役现在买不到额外的东西:
   > 硬读烧 token 那个动机已经由阶段 1.1(速查表自足)解决了。
3. **P15 阶段 0 → 3**(`core/` 勘误 + 制度化)。阶段 0 不需要拍板;
   阶段 2 与 P11 阶段 3 撞 `core/11`,跟在 P11 后面做。
   **阶段 3 的三条制度动作是它的主要产物**,防的是下一个计划完结时再犯同一批错。
4. **P16 阶段 0 → 5**(线索引擎 + 三线索检验)。`roll.py` 已落地,**"排在 P14 之后"这条
   前置已解除**;仍建议**排在 P11/P15 之后**(阶段 4 三方撞 `core/11`),阶段 0–3 与那三条
   都不撞,可以插做。
   它和 P14 是同族诊断,但**修的东西不同**:P14 治"模组雷同",P16 阶段 3–4 治的是
   **"三条线索齐了照样卡关"**——现在 `core/11` 只数行数,六种失败一种都数不出来,
   其中"三条线索全会随时钟腐坏"那条**会随场次自己恶化**,是这批计划里唯一一条越拖越贵的。
5. **P10 阶段 1.0 采料**(读 9 段区导言,只填四维抽象格)→ **阶段 1.1–1.2**(提炼稿 +
   `core/03` 两处接线)→ **阶段 2**(d20 机构表 + `diagram-conventions-zh.md` 扩 §六
   与 3 张样例)。零阻塞,做完即可归档。
   **阶段 2 里有一条债优先**:`diagram-conventions-zh.md:69` 的「文字地图卡」是个从未
   定义过的裸名词,§六 落地即还清——与 P5 阶段 0 还的是同一类 bug。
   > 两条复评没抓到的:①**100 行配额对四节而言偏紧**(§二 光门牌法则就 6 条规则),
   > 建议配额改成每节上限,并把 1.0 那句"采完确认料够不够"提到 1.1 的验收标准;
   > ②**复评修订 A 的第一条理由已随 P13 失效**(「不进 bundle 等于目标用户拿不到」)
   > ——结论不变,另外三条理由站得住,只是这条要换。
6. **P5 阶段 1**(A 档定位图)——先出原型再定稿惯例,别先写模板。
   > ⚠️ **这一步现在没有输入,而状态表写的是"无阻塞"。** 原定样本
   > `campaigns/beidaihe-winter/` 已于 2026-08-04 删除,计划自己标注了"仓库里没有在跑的
   > 战役,样本得先写一份"——也就是实际第一步是**先造一个战役**,而这件事没有 checkbox、
   > 没有工作量估计,也没说它是临时 scratchpad 还是入库的示例战役。**开工前先答这个。**
   > 另:**P5 阶段 3(降级路径)的前提要重新确认**——它假设存在一条"无文件系统的
   > ChatGPT 网页链路",而 `core/00:173` 已写死 kit 由**有文件系统的 agent 就地读**。
   > 该链路若已不存在,阶段 3 可能整块不必做;**这同时决定 P10 建筑卡那张的存废理由**
   > (P10 修订 B:"砍掉它 ChatGPT 链路就彻底没有室内地图")。**两者一起答,别分两次。**

**已完成的历史条目**(2026-08-04,不再占编号):P11 阶段 0 + 1 + 2(六个年代包已建,
`eras/README.md` 枢纽含未覆盖年代的推导路径)· P10 阶段 0(docx 已转
`reference/_source/arkham-zh.md`,6723 行 / 14 万字符 / 0 图;**决定不切分**,标题体系
够规整,跳读比切文件便宜)· P10 三个拍板问题(全文**不**归档、d20 机构表**做**、
文字地图卡 3 张全原创)· P5 阶段 0(mermaid 惯例落地,还掉 `templates/cult.md` 与
`craft/cult-design-zh.md` 两处指向不存在规范的引用)。

**P14 阶段 1**(2026-08-05,148bb91):`scripts/roll.py` + ground rule + 三适配器同步 +
五处掷骰点措辞 + `core/11` 审查项,逐条见计划文件「阶段 1」小节。阶段 2–6 仍待执行。

~~**P13 bundle 退役**~~ —— **已完成 2026-08-04 并归档**。它解掉的那层顾虑现在是既成
事实:归档件就在仓库里,spec 直接依赖即可,不必再写 `if present locally`。

## 复杂度排序(从简单到复杂)

| # | 计划 | 复杂度 | 工作量实测 | 为什么是这个档 | 现在能动 |
|---|---|---|---|---|---|
| 1 | **P10** 城镇解剖手法稿 | ★★☆☆☆ | 资料 14 万字符 / 6723 行 / 0 图;采料 10 段 + ≤100 行提炼稿 + `core/03` **两处**接线 + d20 机构表 + `diagram-conventions-zh.md` 扩一节与 3 张样例 | 产出本身小(照 `lovecraft-zh.md` 先例)。**2026-08-04 降一档并升到本表第一**:阶段 0 完成后发现资料自带标题体系,提炼 §一只要读 9 段区导言、不必碰 300 个地点条目;同日三个拍板问题全部定案,其中"归档全文"被硬约定直接否掉,反而少一份活。**同日复评后仍是 ★★**,但工作量重新分布了:地图卡改成扩既有文件(少一个新文件),而 d20 表比原估贵——"分类学现成"只覆盖 20 行中的 8 行,另 12 行要把 163 条「各行各业」重做子分类。难点仍是"取手法不取文字"的边界,挡法已写死进阶段 1 与阶段 2 | ✅ 全程可动 |
| 2 | **P14** 模组多样性 | ★★☆☆☆ | `scripts/roll.py`(~200 行:表解析 + 不放回 + 跨战役查重 + 行数自检)+ 两张新 d20 表(共 40 条原创条目)+ `core/00` 一条 ground rule 与三适配器同步 + 5 处掷骰点改措辞 + `core/11` 一条审查项 + 战役新增 `rolls.log` | **2026-08-04 从 ★★★★ 降到 ★★**:原方案「390 条纯创作」被推翻,剩下的主体是一个**当场可验证**的脚本——跑一千次看分布,比任何评审都硬;原创写作只剩 40 条。★ 不高不代表不要紧,它是这批计划里唯一修「现在就在出错」的机制的一条。真风险不在技术在执行:模型会不会真去跑脚本,靠 `rolls.log` 落盘 + `core/11` 判 fail 兜底 | ✅ 全程可动 |
| 3 | **P11** 年代开放 | ★★★☆☆ | 资料 52 页 / 4 图;1 个基准声明 + 全部年代 delta + `eras/README.md` 枢纽 + `core/01`/`core/02`/`core/11` 三处接线 | 资料小、约定干净(规则内容可转录),**且没有阻塞项**。难点不在建包(几页一个年代),在 `eras/README.md` 要把「差集写法」定得够死——书里没有的年代靠它现场推导 | ✅ 全程可动 |
| 4 | **P12** 速查表自足化 | ★★★☆☆ | 源材料 2,402 行 / 21.3 万字符(第六、七、八章)+ 其余四章;4 份重写 + 2 份新建(恐惧症/躁狂症 d100、武器强度索引)+ 8 处引用改写 + 1 处脚本改动 + `.gitignore` + 索引重建 | **实现不难,难在刹车**:「一字不落」没有天然上限,靠阶段 0 的样例量膨胀比来定死(bundle 退役后体积不再是约束,但「速查表」三个字仍然是)。**勘察已做完,错误清单逐条核过行号**——`chases.md` 整份机制是错的、`madness.md` 的 d10 表和书上不是同一张表,这两条不是「提炼太简」,是必须推倒重写。**三问已定案,不再有决策成本** | ✅ 全程可动 |
| 5 | **P5** 低成本地图 | ★★★☆☆ | 阶段 0 一份惯例文件 + 两处改引用;阶段 1 新写 `scripts/render-map.py`(A 档,~200 行)+ 原型验证 + 2 个模板 + 2 个 core 各加一行;阶段 2 家具图元 + `--player` 开关;阶段 2b 折线 + 指北针(~20 行) | 唯一要**从零写渲染器**的计划,成品好坏得肉眼看 SVG。**但 2026-08-04 连降两次**:先是 C/D 否决掉材质、阴影与手绘抖动(渲染器变纯确定性);再是照阿卡姆室内图样本发现**家具用几何图元 + 标签就够,不需要图标库**,B 档少掉一半代码和一份会永远增长的美术资产。室外站点图也确认**复用同一渲染器**,不另起炉灶。**阶段 0 单独看只有 ★☆,而且是在还债** | ✅ 已定案,规格有样本可对 |

**读法**:★ 数看的是「一个新窗口从零上手到能提交」的总成本——阅读量、决策量、
文件数、能否当场验证,四项合起来。它和「重要性」无关。

**两条经验**(从本表的形状读出来的):

- **能当场验证的排前面。**
- **待讨论的计划,复杂度看"决策"不看"实现"。** 

**P16 同样未编进上表**(2026-08-04 新立)。按同一口径它落在 **★★☆☆☆**:产物量小
(一张 10 条的新表 + 5 处 spec/模板改口径 + 2 处新增审查项,无脚本、无新目录),
**难点全在措辞与刹车**——十台引擎写成并列清单就等于什么都没改(风险 1),
六条检验写进 `core/04` 容易把第 5 步撑成一页(风险 2)。**它有一半是"能当场验证"的**:
引擎表拿两个不同模组各掷一次,组合该几乎没有交集,按本表第一条经验加分;
另一半(六条检验管不管用)要等一个真战役过一遍 review 才知道。全程可动,
但**阶段 4 建议让 P11/P15 先过 `core/11`**。

**P15 未编进上表**(2026-08-04 新立),避免为一行重排五行。按同一口径它落在 **★★☆☆☆,
与 P10/P14 同档**:阶段 0 是纯文本勘误(最便宜的一档),阶段 1 若选"扩索引"要动
`build_monster_index()` 并跑 223 条全表回归(**能当场验证**,按上面第一条经验加分),
阶段 3 改的是完结清单与 `core/15` 本身——**难点不在实现在措辞**:制度动作写松了不起作用,
写死了会给以后每个计划加税。全程可动。

## 执行守则

- **一次只把一个计划标为"进行中"**,避免半成品互相踩(P1 逐章确认的等待期
  可以插做无依赖项,插做时状态写明:如"进行中(等 Keeper 确认第二章;插做 P6)")
- 每完成计划内一个条目:勾掉该计划文件里的 `- [ ]`
- 每完成一个阶段/整个计划:走一遍下面的**完结清单**
- 收尾在**每个计划完结时**做,不留到最后一起
- 新计划:新建 `YYYY-MM-DD-<slug>.md`,本表加一行,依赖图补节点

## 完结清单(Definition of Done)

**每个计划完结时逐条走完再提交。** 漏项的典型后果:归档文件头还写着"待执行"
(2026-08-02 P2 就是这么漏的)、索引与 `reference/` 不同步、README 入口说明过期。

### 1. 状态同步(两处,必须一致)

- [ ] 计划文件头的 `> 状态:` 改成 `**已完成(<commit>)**`,阻塞项写清等什么
- [ ] 本文件状态索引表的「状态」列同步,填 commit hash
- [ ] 计划内所有 `- [ ]` 勾掉;**没做的条目不许勾**——要么写明放弃原因,
      要么拆成新计划并在索引表加行

### 2. Changelog

- [ ] 在根 `CHANGELOG.md` 记一条(格式见该文件顶部)。写**面向 Keeper 的变化**
      ——"现在能做什么/必须怎么做",不是"改了哪个文件哪行"
- [ ] **当天已有条目就补进那一条**(在 `### 修复问题` / `### 更新内容` 下各加一句话、
      标题追加 commit),不要为同一天新开一条
- [ ] 改动影响到根 `README.md` 的入口说明(快速开始、流程图、技能表)时一并更新

### 3. 产物重建

- [ ] 动过 `templates/investigator.schema.json` → 重跑 `scripts/render-investigator.py`
      验证卡面还能渲染
- [ ] 动过 `reference/decks/`、`reference/sourcebooks/`(增删改归档件或其引用出处)→
      重跑 `python scripts/build-reference-index.py`,**必须报 no problems**;
      报「orphaned」说明归档件没接进任何 spec,先接线再提交(见 `core/14-archive-reference.md`)
- [ ] 改动了目录结构、硬约定或计划状态 → 更新 `WORKLOG.md`(给接手会话的上手速览)

### 4. 三适配器一致性

- [ ] 行为改动只落在 `core/`;**根适配器不放独有指令**
- [ ] 确需改适配器时,`CLAUDE.md` / `GEMINI.md` / `AGENTS.md` **三份同步改**
      ——只改一份是 bug,另两个模型不会照做
- [ ] 新增/更名技能:`.claude/skills/<name>/SKILL.md` 与 `CLAUDE.md` 技能表两处都改

### 5. 术语与语言

- [ ] 引入新的规则术语 → 进 `reference/glossary-zh.md`
- [ ] kit 脚手架与文件名保持英文;中文只出现在生成内容与本目录的计划文档

### 6. 计划间关系

- [ ] 本计划**解锁**的下游节点:在依赖图里去掉箭头,被解锁的计划状态从
      `阻塞` 改回 `待执行`
- [ ] 本计划暴露的新问题 → 新建计划文件,别塞进已完结的计划

### 7. 提交与归档

- [ ] commit 信息与 changelog 条目对得上;拿到 hash 后**回填**状态表和计划文件头
      (hash 是提交后才有的,这两处必然是二次提交或 amend)
- [ ] 整个计划(不是单个阶段)完结后,把文件移进 `Archived/`;**本文件状态索引表
      对应行整行删除**(只列未完成的计划),范围描述挪进 `Archived/README.md`
      并加一行索引——细节别留在本文件
- [ ] 提交前 `git status` 确认没有漏掉 `.claude/` 下的联动改动

## 已完成归档

计划全部完成后把状态标 `已完成(<commit>)`,移动至 `update_plan/Archived/`,
作为设计决定的记录(各计划内含"为什么这样做"的备忘)。
**归档不等于完结**——先走完上面的完结清单,最后一步才是移动文件。

**归档计划的范围描述、设计理由等细节记在 [`Archived/README.md`](Archived/README.md),
不在本文件重复。** 本文件的状态索引表**只列未完成的计划**,归档条目整行删除
——理由是归档计划只会越来越多,留着指针行也会让每次读本文件的 token 成本一直涨。
新增归档时,除了走上面的完结清单,还要在 `Archived/README.md` 加一行索引。
